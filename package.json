{
  "name": "todo-app-with-authentication",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-server": "npm --prefix=backend run start",
    "dev-client": "npm --prefix=client run dev",
    "dev": "npm-run-all --parallel dev-server dev-client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
